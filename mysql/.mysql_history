use mysql;
select user,host from user;
use mysql;
select user,host from user;
flush privileges;
show databases;
use uki_db;
show tables;
select * from users_tab;
use mysql; # use mysql table
update user set authentication_string=PASSWORD("") where User='root'; # update password to nothing
update user set plugin="mysql_native_password" where User='root'; # set password resolving to default mechanism for root user
flush privileges;
quit;
show databases;
CREATE USER 'dev' IDENTIFIED BY 'palma';
GRANT USAGE ON *.* TO 'dev'@localhost IDENTIFIED BY 'palma';
FLUSH PRIVILEGES;
GRANT ALL privileges ON *.* TO 'dev'@localhost;
FLUSH PRIVILEGES;
SHOW GRANTS FOR 'dev'@localhost;
GRANT USAGE ON *.* TO 'dev'@localhost IDENTIFIED BY 'palma';
FLUSH PRIVILEGES;
show databases;
DROP user dev;
use mysql;
DROP user 'dev'@'localhost';
DROP user 'dev';
use mysql;
select User,Host from user;
CREATE USER 'dev'@'localhost' IDENTIFIED BY 'palma';
GRANT USAGE ON *.* TO 'dev'@'localhost' IDENTIFIED BY 'palma';
GRANT ALL privileges ON *.* TO 'dev'@'localhost';
FLUSH PRIVILEGES;
select User,Host from user;
show databases;
ifconfig
;
uninstall plugin validate_password;
show databases;
GRANT ALL PRIVILEGES ON *.* TO 'debian-sys-maint'@'localhost' IDENTIFIED BY 'palma';
FLUSH PRIVILEGES;
GRANT USAGE ON *.* TO 'dev'@'%' IDENTIFIED BY 'palma';
FLUSH PRIVILEGES;
GRANT ALL privileges ON *.* TO 'dev'@'%';
FLUSH PRIVILEGES;
GRANT USAGE ON *.* TO 'dev'@'%' IDENTIFIED BY 'palma';
GRANT ALL privileges ON *.* TO 'dev'@'%';
FLUSH PRIVILEGES;
CREATE USER 'dev'@'%' IDENTIFIED BY 'palma';
use mysql;
select User,Host from user;
FLUSH PRIVILEGES;
ls
;
show databases;
    set @@global.max_connections = 400;
set @@global.max_connections = 400;
SET GLOBAL max_allowed_packet = 1024 * 1024 * 256;
use mysql
select user,host from user;
